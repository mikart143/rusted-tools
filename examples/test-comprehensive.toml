# Comprehensive test configuration for rusted-tools MCP proxy

[http]
host = "127.0.0.1"
port = 3000

# Knowledge Graph / Memory Server (Node.js)
[[endpoints]]
name = "memory"
type = "local"
command = "npx"
args = ["-y", "@modelcontextprotocol/server-memory"]
auto_start = true

# Web Fetch Server (Docker)
[[endpoints]]
name = "fetch"
type = "local"
command = "docker"
args = ["run", "--rm", "-i", "mcp/fetch"]
auto_start = true
