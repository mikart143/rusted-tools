# Test configuration with Docker MCP server

[http]
host = "127.0.0.1"
port = 3000

[[endpoints]]
name = "fetch"
type = "local"
command = "docker"
args = ["run", "--rm", "-i", "mcp/fetch"]
path = "fetch"
auto_start = true
restart_on_failure = true
