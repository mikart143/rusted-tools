# Test configuration for rusted-tools proxy

[server]
host = "127.0.0.1"
port = 3000

[[mcp_servers]]
name = "memory"
type = "local"
command = "npx"
args = ["-y", "@modelcontextprotocol/server-memory"]
path = "memory"
auto_start = true
restart_on_failure = true
